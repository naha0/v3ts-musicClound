export interface IBannerList {
  imageUrl: string;
  typeTitle: string;
  targetId: number;
}
export interface IPersonalized {
  id: number;
  name: string;
  picUrl: string;
  playCount: number;
}
